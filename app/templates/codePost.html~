<div name=post>
    <span name=author>
        {{ post.author }}
    </span>
    <span name=post>
        {% if post.codeType=="html" %}
            {{ post.text|safe }}
            {#        {% elif post.codeType=="python" %}
            {{ post.text|safe }}
            {% else %}#}
            {{ post.text }}
        {% endif %}
    </span>
</div>
<br /> 
